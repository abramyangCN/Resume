name: Deploy Resume

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  models: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build resume.json from YAML sources
        run: node build.js

      - name: Update GitHub Gist
        env:
          GIST_TOKEN: ${{ secrets.GIST_TOKEN }}
          GIST_ID: ${{ secrets.GIST_ID }}
        run: node scripts/update-gist.js

      - name: Generate README
        run: node scripts/generate-readme.js

      - name: Commit updated README
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --staged --quiet || git commit -m "chore: update README [skip ci]"
          git push origin HEAD

      - name: Export resume to HTML
        run: |
          pnpm exec resume export resume.html --theme even
          mkdir -p dist
          cp resume.html dist/index.html

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: dist

      # - name: Trigger iay.me redeploy
      #   env:
      #     IAY_DEPLOY_TOKEN: ${{ secrets.IAY_DEPLOY_TOKEN }}
      #     IAY_REPO: ${{ secrets.IAY_REPO }}
      #   run: |
      #     curl -s -X POST \
      #       -H "Authorization: Bearer $IAY_DEPLOY_TOKEN" \
      #       -H "Accept: application/vnd.github+json" \
      #       -H "X-GitHub-Api-Version: 2022-11-28" \
      #       "https://api.github.com/repos/$IAY_REPO/dispatches" \
      #       -d '{"event_type":"resume-updated"}'
      #     echo "✓ Triggered iay.me redeploy"

      - name: Translate resume to Chinese
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node scripts/translate.js

      - name: Commit translated resume
        run: |
          git add resume.zh.json
          git diff --staged --quiet || git commit -m "chore: update resume.zh.json [skip ci]"
          git push origin HEAD
